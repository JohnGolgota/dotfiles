common_setup="${XDG_CONFIG_HOME:-$HOME/.config}/shell_setup/common"
if [ -d "$common_setup" ]; then
    for rc in "$common_setup"/*; do
        if [ -f "$rc" ]; then
            source "$rc"
        fi
    done
fi
unset rc

